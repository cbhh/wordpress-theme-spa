export default interface PageMetaModuleTypes {
    /**
     * 标题
     */
    title: string;
    /**
     * 时间，仅在post视图上出现
     */
    time?: string;
    /**
     * 背景图片URL，仅在post视图上出现
     */
    background?: string;
}
